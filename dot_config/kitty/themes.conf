# give `kitten themes` a dedicated writable include without touching kitty.conf.
# Kitty theme bootstrap.
# `kitten themes --config-file-name themes.conf` rewrites the block below.
# BEGIN_KITTY_THEME
# Monokai
include current-theme.conf
# END_KITTY_THEME
