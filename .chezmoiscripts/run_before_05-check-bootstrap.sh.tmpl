#!/bin/sh
# Prevent direct applies from bypassing the controller's prerequisite checks.
set -e

# The controller (and its compatibility bootstrap wrapper) sets this flag.
if [ -z "$CHEZMOI_BOOTSTRAP_MODE" ]; then
  echo "ERROR: Run 'dotfiles apply' instead of 'chezmoi apply' directly"
  exit 1
fi
