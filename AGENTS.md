# AI change comments

Every change made by an AI must include at least one nearby, plain-language comment explaining the reason for the change or the behavior it preserves, even when it seems obvious. Write it as a natural comment without a `Why:` label. Keep comments brief and focused on behavior; do not add exhaustive symbol documentation or LSP-style documentation. The user may prune these comments later.
